#include <stdio.h>

int main ()
{
  // printf("Hello World!\n") //error: expected ';' before 'return'
  //  printf"HelloWorld!\n"); // expected ';' before string constant (points to " between printf and HelloWorld
  //expected statement before ')' token

  //  printf(Hello World!\n"); // 'Hello' undeclared (first use in this function); did you mean 'ftello'?
  // each undeclared identifier is reported only once for each function it appears in
  // stray '\' in program
  // missing terminating " character
  // expected ';' before '}'
  return 0;
}
